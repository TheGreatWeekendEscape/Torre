import game.Run;
import model.entity.Player;
import util.Console;

import static util.Console.*;

public class Main {
    public static void main(String[] args) {
        Player p = new Player("");
        Run r = new Run(p);
    }

}